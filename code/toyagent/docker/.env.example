# ============================================
# Docker Compose Orchestration Configuration
# ============================================
# This file contains ONLY infrastructure and orchestration config.
# Application-specific config is in backend/.env.docker and frontend/.env.docker
#
# Copy this file to .env and update with your actual values:
#   cp .env.example .env

# ============================================
# PostgreSQL Database Configuration
# ============================================
# Service name in Docker network (don't change unless you know what you're doing)
POSTGRES_PORT=5432

# Database credentials (shared by both main app and Temporal)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=  # Generate: openssl rand -base64 32

# Main application database
POSTGRES_BACKEND_DB=backend

# Temporal database (uses same user as above)
# Temporal auto-setup will create this database automatically
POSTGRES_TEMPORAL_DB=temporal

# ============================================
# Temporal Server Configuration
# ============================================
# Temporal Web UI
TEMPORAL_UI_PORT=8080

# ============================================
# Ports (Host:Container)
# ============================================
POSTGRES_PORT=5432
TEMPORAL_PORT=7233
